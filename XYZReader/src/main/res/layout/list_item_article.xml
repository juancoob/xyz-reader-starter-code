<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/min_margin"
    android:layout_marginLeft="@dimen/min_margin"
    android:layout_marginRight="@dimen/min_margin"
    android:layout_marginTop="@dimen/min_margin"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!--suppress AndroidLintContentDescription -->
        <com.example.xyzreader.ui.DynamicHeightNetworkImageView
            android:id="@+id/thumbnail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/photo_placeholder"
            android:fitsSystemWindows="true"
            android:scaleType="centerCrop" />

        <TextView
            android:id="@+id/article_title"
            style="@style/TextAppearance.AppCompat.Subhead"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="4"
            android:paddingBottom="@dimen/min_padding"
            android:paddingLeft="@dimen/tv_default_padding"
            android:paddingRight="@dimen/tv_default_padding"
            android:paddingTop="@dimen/tv_default_padding"
            android:textColor="@color/primary_text_color"
            tools:text="Title" />

        <TextView
            android:id="@+id/article_subtitle"
            style="@style/TextAppearance.AppCompat.Body1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:paddingBottom="@dimen/tv_default_padding"
            android:paddingLeft="@dimen/tv_default_padding"
            android:paddingRight="@dimen/tv_default_padding"
            android:textColor="@color/secondary_text_color"
            tools:text="Subtitle" />
    </LinearLayout>

</android.support.v7.widget.CardView>
